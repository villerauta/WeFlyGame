using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

/*namespace WeFly
{
    [CustomEditor(typeof(BaseAirplane_Input))]
    public class BaseAirplaneInput_Editor : Editor
        {
            private BaseAirplane_Input targetInput;
            
            void onEnable() {
                targetInput = (BaseAirplane_Input)target;
            }

            public override void OnInspectorGUI()
            {
                base.OnInspectorGUI();

                string info = "";
                info += "Pitch: " + targetInput.Pitch + "\n";
                info += "Roll: " + targetInput.Roll + "\n";
                info += "Yaw: " + targetInput.Yaw + "\n";
                info += "Throttle: " + targetInput.Throttle + "\n";
                info += "Brake: " + targetInput.Brake + "\n";
                info += "Flaps: " + targetInput.Flaps + "\n";
            
                GUILayout.Space(20);
                EditorGUILayout.TextArea(info,GUILayout.Height(200));
                
            }
        }
    
}*/
